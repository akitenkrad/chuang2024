[project]
name = "chuang2024-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "chuang-tools",
]

[tool.uv.workspace]
members = ["tools"]

[tool.uv.sources]
chuang-tools = { workspace = true }
